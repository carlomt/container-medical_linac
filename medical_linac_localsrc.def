Bootstrap: docker
From: carlomt/geant4:11.1.0-almalinux9

%files
	./medical_linac /workspace/source

%post
	cd /workspace/ && \
	cmake /workspace/source && \
	make -j`nproc` && \
	echo /workspace/medical_linac > /usr/local/bin/run && \
	chmod +x /usr/local/bin/run
	
%environment
	
	
%runscript

